# Port Digital Twin Environment Configuration

# MongoDB Configuration (update with your existing MongoDB details)
MONGODB_URI=mongodb://admin:admin123@host.docker.internal:27017/enerspectrumSamples?authSource=admin

# JWT Secret (change this to a strong secret for production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Frontend URL (update with your actual domain)
FRONTEND_URL=http://localhost:3000
NEXT_PUBLIC_BACKEND_URL=http://localhost:5001

# Email Configuration (for user verification)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# API URLs for services
DC_POWER_FLOW_API=http://dc-power-flow:5001
VESSEL_API=http://vessel-modelling:5002 